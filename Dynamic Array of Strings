
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    string* arr = new string[n];

    for(int i = 0; i < n; i++)
        cin >> arr[i];

    for(int i = 0; i < n; i++)
        cout << arr[i] << endl;

    delete[] arr;
}
